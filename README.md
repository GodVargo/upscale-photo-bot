# Upscaler Photo Bot

Telegram-бот для апскейла фотографий с помощью AI.

## 📌 Возможности

- 🖼️ Увеличение разрешения фото 2x / 4x
- 🎨 Улучшение чёткости и деталей
- 🔇 Удаление шумов (3 уровня)
- 🤖 AI-обработка через DeepAI (waifu2x)
- 📊 Статистика и админ-команды

## 🛠️ Технологии

- **Bot**: Python + aiogram 3.17
- **Database**: PostgreSQL
- **WebApp**: HTML + JavaScript
- **API**: DeepAI waifu2x (бесплатно)

## 🚀 Деплой на Railway

1. Создайте проект на [Railway](https://railway.app)
2. Добавьте PostgreSQL через "New" → "Database" → "PostgreSQL"
3. Подключите GitHub репозиторий
4. Добавьте переменные окружения:
   - `BOT_TOKEN` — токен бота
   - `ADMIN_ID` — ваш Telegram ID
   - `WEBAPP_URL` — URL WebApp на GitHub Pages
   - `DATABASE_URL` — автоматически из Railway

## 📁 Структура

```
UscalerPhoto/
├── bot.py           # Основной бот
├── .env             # Конфигурация (локально)
├── requirements.txt # Зависимости
├── Procfile         # Railway
├── runtime.txt      # Python версия
└── webapp/
    ├── index.html   # WebApp интерфейс
    └── app.js       # Логика апскейла
```

## 👮 Админ-команды

- `/stats` — Статистика бота
- `/export` — Скачать CSV пользователей
- `/broadcast Текст` — Рассылка всем

## 📄 Лицензия

MIT
